#ifndef SCAM_GUI_INTENSITY_HEADER
#define SCAM_GUI_INTENSITY_HEADER

extern const char * intensity_colors[5];

enum Intensity {
   None = 0,
   Low = 1,
   Moderate = 2,
   High = 3,
   Sick = 4,
};

#endif
